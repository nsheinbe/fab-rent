# fab.rent — working notes for Claude

Premium rental marketplace for equipment, tools and event supplies (demo market: Port Maren, currency Maren dollar `$` / MRD). Built from the finished design in `./design/*.dc.html` — those files are the source of truth for layout, copy and demo data. `design/frames/` and `design/support.js` are viewer scaffolding; ignore them.

## Stack

- **Next.js 16** (App Router, Server Components, Server Actions) · **TypeScript strict** (`noUncheckedIndexedAccess`) · **Tailwind CSS 4** with the design tokens as CSS variables in `@theme` (`app/globals.css`). pnpm.
- **Postgres / Supabase**: schema, RLS and functions live in `supabase/migrations/*.sql`; demo data in `supabase/seed.sql` (generated by `supabase/seed/generate.ts`). Generated DB types in `lib/db/types.generated.ts` (`pnpm db:types`, kysely-codegen).
- **Data access**: server-side SQL through **Kysely + pg** against `DATABASE_URL` (plain Postgres locally, or the Supabase Postgres connection string). Every request runs inside `withActor()` which opens a transaction, sets `request.jwt.claims` and `SET LOCAL ROLE anon|authenticated|service_role`, so **RLS policies are enforced for server code too** (the pattern Supabase documents for Drizzle/Kysely). `supabase-js` / `@supabase/ssr` are used for Auth, Storage and Realtime when `NEXT_PUBLIC_SUPABASE_URL` is set.
- **Demo mode** (no Supabase env): cookie sessions signed with `APP_SECRET`, sign-in as any seeded demo account or via email OTP whose code is logged to the console; photos stored under `.data/storage/<bucket>/` and served through `/api/storage/*` with HMAC "signed URLs" for private buckets; chat polls instead of Realtime. Same SQL, same RLS.
- **Radix UI** primitives (dialog, popover, select, tabs, slider, switch, checkbox, radio, toast) under a small in-repo component library. **Zod 4** for every input. **date-fns / date-fns-tz**. **MapLibre GL** for maps (falls back to the design's "Map tiles · demo" pattern when tiles can't load). **pdf-lib** for receipts. **Vitest** (unit) + **Playwright** (e2e, 390 & 1280).
- Payments: `PaymentProvider` interface (`authorize`, `capture`, `charge`, `release`, `refund`) with `MockPaymentProvider` (default, deterministic) and `StripePaymentProvider` behind `PAYMENTS_PROVIDER=stripe` (PaymentIntents, `capture_method: 'manual'` for holds). A hold is never modelled as a charge.
- ID verification: `IdVerificationProvider` interface with a mock that flips `id_verified` after a fake 2 s check.

## Folder layout

```
app/
  (renter)/        home, search, listings/[slug], book/[listingId], auth, checkout/[draftId],
                   bookings/[ref]/confirmed, rentals, rentals/[ref](/extend|/review), inbox, saved, profile
  (provider)/provider/   dashboard, bookings, listings(/[id]), calendar, handoff/[ref], return/[ref], earnings,
                   inventory, messages, reviews, settings
  (admin)/admin/   overview, users, listing-review(/[id]), disputes(/[code]), settings, bookings, payouts, reports
  styleguide/      reproduces the Overview design page
  api/             route handlers only for webhooks, uploads/storage, cron, receipts, .ics
components/ui      Button, Input, Select, Textarea, Stepper, Sheet, Dialog, Tabs, SegmentedControl, DataTable,
                   SidePanel, Avatar, StarRating, PhotoSlot, Calendar, Timeline, Toast …
components/domain  StatusPill, AvailabilityPill, FulfillmentPill, ListingCard, PriceBreakdown, DateLocationSummary,
                   BookingBottomBar, BottomTabBar, ProviderShell, AdminShell …
lib/pricing        pure money functions (config + listing + inputs → cents)
lib/booking-state  transitionBooking() — the only way a booking status changes
lib/listing-checks automated listing checks + review routing
lib/payments       PaymentProvider + Mock + Stripe
lib/auth           actor resolution, demo/supabase auth providers, withActor()
lib/db             Kysely client, generated types, query helpers
lib/storage        Supabase Storage / local disk adapters
lib/settings       MarketplaceConfig schema, live-config loader
lib/time           now() (honours DEMO_NOW), market time zone helpers
lib/format         formatMoney(cents), formatDateRange(), …
supabase/migrations  SQL migrations (RLS on every table)
supabase/seed.sql    generated demo data (Port Maren, anchored to DEMO_NOW)
supabase/seed/       TS generator
supabase/local/      auth-schema shim for plain Postgres (never applied on Supabase)
tests/unit           vitest · tests/e2e  playwright
scripts/             db-reset, db-types
```

## Conventions

- Server Components by default; `"use client"` only for interaction. Mutations are Server Actions in `app/**/actions.ts`, validated with Zod and wrapped in `withActor()`.
- Money is **integer cents** everywhere (`bigint`/`integer` in SQL, `number` in TS). Format only at the edge with `formatMoney(cents)`. Rounding: half-up at the cent (`roundCents`).
- Dates are `timestamptz`; all display in the market time zone (`config.market.timezone`, label "Maren Standard", week starts Monday) via `lib/time`. One `formatDateRange()`.
- `now()` from `lib/time` returns `DEMO_NOW` (market-local ISO) when set, real time otherwise. The seed is generated relative to the same anchor.
- Status colours and copy come from the single `bookingStatus` map in `lib/booking-state/status.ts`. Never inline a pill colour.
- No hard-coded fee, tax, hold, waiver, cancellation, SLA or payout numbers in components — always from the live settings version (or the booking's `price_snapshot` / `settings_version`).
- Copy is the design's copy. Where the design shows a demo value, it is seed data, not a string constant.
- Every list has loading, empty and error states.
- DM Mono for booking refs, serials and amounts inside tables.

## Money rules (§5 of the brief — must match the design to the cent)

- **Billed days** = `ceil((end − start) / 24 h)`; any part of a 24 h period counts as a day (`Fri 09:00 → Sun 17:00` = 3; `Sat 11:00 → Mon 11:00` = 2). Min/max days from the listing.
- **Rate selection** (lowest wins): `day × days`; **weekend** rate replaces it when the span starts Friday and ends the following Monday; **week** rate from 5 days (whole weeks at week rate, remainder at day rate, a ≥5-day remainder is itself a week); **month** from 28 days.
- **Extras**: `per rental` once; `per day` × billed days. The damage waiver is an extra that also lowers the hold to `hold_with_waiver_cents`.
- **Delivery**: `base_cents` up to `base_km` (5 km), then `per_km_cents` per additional km, rounded up per km; includes collection.
- **Renter service fee**: `renter_fee_pct` (10 %) of the **rental charge only**, min $2.00, cap $150.00, own line.
- **Sales tax**: `sales_tax_pct` (7.5 %) on rental + service fee + delivery + extras.
- **Charged now** = rental + delivery + extras + service fee + tax.
- **Hold** = listing hold, or `hold_with_waiver` if the waiver is bought; never above `max_hold_cents` ($2,500); placed at handoff (after the condition record); auto-released 3 business days after return check-in; only becomes a charge when a claim is accepted or upheld.
- **Provider commission**: `provider_commission_pct` (12 %) of rental + extras — never on delivery, never on claims. Payout = rental + extras + delivery − commission. Funds clear at return check-in; weekly payout (Tue), min $25; paused when tax ID or bank unverified.
- **Late fee**: `late_fee_cents_per_hour × floor(whole hours beyond grace)`. `2 h 10 m late, 1 h grace, $15/h → $15.00`.
- **Extension**: extra days × day rate + service fee + tax; charged only when the provider approves; hold unchanged; collection window shifts.
- **Cancellation**: Flexible free until 24 h before start, then 50 % of rental kept; Moderate free until 72 h, 50 % then 100 % inside 24 h; Strict free until 14 days, 50 % then 100 % inside 7 days. Fee and tax on the refunded portion are always returned. Nothing is ever held on a cancelled booking. Provider cancellation = full refund + $20 credit (10 % credit on Strict). Not available after handoff.
- **Claims**: raised only at return check-in; undisputed late fee settles immediately (separately from the hold); renter has 48 h; admin SLA 48 h; wear-and-tear allowance 20 % on tools > 2 years; full uphold only with matched-angle photo pairs; upheld amount goes to the provider with no commission, remainder of the hold released to the renter. One appeal within 7 days.
- **Instant book**: renter has verified ID **and** (rating ≥ 4.5 **or** ≥ 3 completed rentals); otherwise `requested`. Same-day bookings blocked after the listing's cutoff.

Test vectors (fixtures in `tests/unit/pricing.test.ts`): DeWalt DWE7491 $58/day · $150 weekend · $210 week · hold $300 · waiver $8/day → hold $100 · 60T blade $12/rental · cord $5/day.
1. Fri 11 Sep 09:00 → Sun 13 Sep 17:00, pickup → rental $174.00, fee $17.40, tax $14.36, **charged $205.76**, hold $300.
2. + delivery 4.2 km ($25.00), blade $12, waiver $24 → fee $17.40, tax $18.93, **charged $271.33**, hold $100; provider gross $235.00, commission −$25.20, payout $209.80.
3. Extension +1 day at $45 → $45.00 + $4.50 + $3.71 = **$53.21**.
4. Return 2 h 10 m late, 1 h grace, $15/h → $15.00; damage $180, no waiver, hold $250 → claim against hold $195.00; partial uphold with 20 % wear → charge $144.00, release $106.00.
5. Settings v41 → v42 (fee 9 % → 10 %) on vector 2: renter pays $269.46 → $271.33; provider payout unchanged.

## Booking state machine (§4)

Statuses (the 11 pills, in order): `requested · confirmed · ready_for_pickup · out_for_delivery · active · return_due · overdue · inspecting · completed · cancelled · disputed`.

`transitionBooking(bookingId, event, actor)` (lib/booking-state) validates the transition, applies side effects, writes `booking_events`, and is the only writer of `bookings.status`.

| from | event | to | notes |
|---|---|---|---|
| requested | `provider_approve` / `instant_confirm` | confirmed | |
| requested | `provider_decline` / `renter_cancel` | cancelled | full refund |
| confirmed | `mark_prepared` | ready_for_pickup | pickup |
| confirmed | `dispatch` | out_for_delivery | delivery |
| confirmed | `renter_cancel` | cancelled | policy applies |
| confirmed | `provider_cancel` | cancelled | full refund + credit |
| ready_for_pickup / out_for_delivery | `handoff_complete` | active | **hold placed here** |
| active | `return_window_open` | return_due | job: within 24 h of `end_at` |
| return_due | `grace_elapsed` | overdue | job: past `end_at` + grace |
| active / return_due / overdue | `return_checkin_start` | inspecting | |
| inspecting | `return_no_claim` / `claim_accepted` | completed | hold released / partially captured |
| inspecting | `claim_disputed` | disputed | |
| disputed | `admin_decision` | completed | decision applied |

Hold auto-release: 3 business days after `returned_at` when no open claim. Card authorisations expire after ~7 days; the admin dispute view shows expiry and offers "extend".

## Running

```
cp .env.example .env.local
pnpm install
pnpm db:reset          # creates the DB, applies supabase/local + migrations + seed.sql (plain Postgres)
pnpm dev               # http://localhost:3000  (DEMO_NOW freezes the clock at Sat 5 Sep 2026 10:00)
pnpm check             # typecheck + lint + unit tests
pnpm test:e2e          # playwright (390 and 1280)
```
With the Supabase CLI: `supabase start && supabase db reset` applies the same migrations + seed; set `DATABASE_URL` to the local Supabase Postgres and the `NEXT_PUBLIC_SUPABASE_*` keys.

## Assumptions & deviations (running list)

1. **No Docker in the build environment** → the app's data path is SQL via Kysely/pg with RLS-enforcing transactions instead of PostgREST. Supabase Auth/Storage/Realtime are used when configured; otherwise demo-mode adapters. Migrations are validated against a local PostgreSQL 16 (`supabase/local/00_auth_shim.sql` provides `auth.users`, `auth.uid()`, `auth.jwt()` and the `anon/authenticated/service_role` roles that Supabase ships out of the box).
2. **Repo README** described an unrelated "local manufacturing" idea; replaced by the fab.rent README in Phase 4.
3. **Demo clock**: `DEMO_NOW = 2026-09-05T10:00` market time (brief §8). Design screens are anchored at slightly different moments (e.g. the Hilti return at 14:10 and the dispute at 14:40 on Sat 5 Sep). To keep the dispute `D-0912` coherent at 10:00 Sat, `FR-9DLA-P2` is seeded one day earlier (due Fri 4 Sep 12:00, returned 14:10, disputed 14:40 → decision due Sun 6 Sep 14:40). A separate Northlands booking is seeded as `overdue` so the dashboard's "Overdue" row and "Returns due today" stat have data.
4. **Market time zone**: "Maren Standard" is mapped to the IANA zone `America/Puerto_Rico` (UTC−4, no DST) so demo times never shift.
5. **Weekend rate** applies only when the span starts on a Friday and ends on the immediately following Monday (vector 1, Fri → Sun, is billed at the day rate).
6. **Week rate remainder**: a remainder of ≥5 days is itself billed as a week (lowest wins); shorter remainders at the day rate.
7. **Billed days** use 24-hour periods, not calendar days (matches every booking in the design: Sat 11:00 → Mon 11:00 = 2 d).
8. **Cancellation policies** live in `config.cancellation.policies`; the `cancellation_policies` table mirrors them (FK target for listings) and is upserted when a settings version is published.
9. **Design totals that are not §5 vectors** (e.g. `FR-2QRX-77 $115.99`) are computed by the pricing engine from seeded rates; where the design's number could not be reproduced from any plausible rate it is treated as decorative and the engine wins.
10. **"Prep due" / "Ready" / "Awaiting provider · ~1 h"** in tables and cards are derived display labels layered on the 11 canonical statuses (one map in `lib/booking-state/status.ts`), not extra statuses.
11. **Calendar drag-to-reassign** ships as click-to-reassign (brief allows this for v1).
12. **Google/Apple sign-in** buttons call Supabase OAuth when configured; in demo mode they sign in as the demo renter (Priya Nair) so the flow can be exercised end to end.
13. **Settings versions**: the brief asks for live v41 = the §5 values *and* a v42 draft "fee 9 → 10 %". Both cannot hold at once (every renter screen is priced at 10 %). Live **v41 = fee 10 %, max hold $2,500** so all renter/provider figures match the design; the seeded draft **v42 = fee 11 %, max hold $3,000** so A05 still opens with 2 unsaved changes, "was …" labels and a live-vs-draft preview. Vector 5 (9 % → 10 %) is covered as a pure-function test.
14. **Quantity**: rental, per-day waiver and the hold scale with `qty`; other extras carry their own quantity (default 1). Hold is capped at `max_hold_cents` after scaling.
15. **Desktop layouts for M10–M14** (rentals, booking detail, extension, inbox, review): the web designs stop at checkout (W04/W05), so these screens reuse the W01 top nav with a centred 560–760 px column (`components/domain/renter-page.tsx`); the detail page splits into timeline + actions columns at `lg`.
16. **Bottom tab bar** is shown only where the mobile designs show it (M01 home, M02 results, M10 rentals) plus the other tab roots (`/saved`, `/inbox`, `/profile`); flow screens (listing, book, auth, checkout, confirmed, detail, extend, thread, review) hide it so their sticky action bars sit at the bottom edge (`components/domain/renter-shell.tsx`).
17. **System-owned tables** (`ledger_entries`, `renter_credits`, `payouts`) are never writable by renters or providers under RLS. Server actions that legitimately produce those rows (checkout → ledger entry, cancellation → credit, return → ledger clearing) run just that statement through `asSystem(trx, …)` inside the same transaction, so the user's own writes stay RLS-checked and atomic.
18. **Reviewer names** on public listing pages come from the `public_profiles` view (id, name, neighbourhood, avatar, ratings, joined) rather than a wider profiles read policy; emails, phones and flags never leave the base table.
19. **Review photos** upload to the private `evidence` bucket under `reviews/<ref>/` (served via signed URLs); message photos go to `condition-photos/messages/<conversation>/`. The listing-photos bucket stays provider-only.
20. **Extension availability** ("Your unit is free until Wed") needs other renters' bookings, which RLS hides; the renter-facing pages read just the next `start_at` as system. Everything else on those pages is RLS-scoped.
21. **Realtime** for the inbox lands in Phase 4; until then the thread refreshes every 15 s and posts optimistically.
22. **Claims** raised at return check-in are answered by the renter on the booking detail page (Accept captures the amount from the hold; Dispute routes to ops). The design has no dedicated renter claim screen, so this is a card in M11's layout.
23. **Provider shell**: the sidebar collapses to the 64 px icon rail on the dense pages the design shows with the "f." mark (bookings, listing editor, calendar, earnings); below `md` the sections become a scrollable chip row under the header (the designs are desktop-only for P01–P04/P07, phone-first for P05/P06).
24. **Approve auto-assigns a unit** when the request is for one unit and none is assigned (first free unit for the span); the panel and calendar can still move it. Multi-unit bookings track a lead unit; the calendar shows unassigned requests under the first unit row.
25. **Extension approval** charges the renter's card immediately for the extension quote (the renter agreed to the priced request), extends `end_at`/`return_due_at`, adds a pending ledger entry (commission on the extra rental only) and posts a system message; declines change nothing.
26. **Late fees settle at return check-in** as a separate card charge (never from the hold), recorded as a settled `late` claim and an `available` ledger entry with no commission; if the charge fails the late fee becomes an open claim against the hold instead. Damage/cleaning/missing claims stay open for `renter_response_hours` with the booking in `inspecting`.
27. **Condition photos, signatures and handoff sheets** live in the private `condition-photos` bucket (`handoff/<ref>/`, `return/<ref>/`); the renter's signature is drawn on a canvas and stored as a PNG. Geotags come from the browser when permitted; timestamps always.
28. **Calendar views**: Week is the designed view (P04); Day is one column, Month is a 28-day strip of the same grid. Drag-to-reassign ships as click-to-reassign (deviation 11); clicking empty space blocks dates for that unit.
29. **Listing edits route through review** when a published listing's title, description, category, day rate or hold changes (§7 edited-listing rules); other fields save directly and apply to new bookings only. Photo hashes are derived from the storage path in demo mode (no image hashing library).
30. **Return checklist areas** are generic (body & housing, working parts, accessories, cleanliness) rather than per-category templates; the provider's issue notes name the part.
31. **Admin console** (A01–A05) is desktop-first; below `md` the sections become the same chip row as the provider shell. Overview "Search → booking" shows "—" until search analytics land (Phase 4); the other health metrics are computed from bookings in the last 30 days (on-time = hold placed within 30 min of start; holds released ≤3 business days ≈ ≤5 calendar days).
32. **Dispute decisions** capture the upheld amount from the hold via `admin_decision` (no commission on claims), mark the claims settled/dismissed, log to `admin_actions`, post a system message to the booking thread and set the appeal window from settings. "Goodwill credit" pays the provider from the platform ledger and releases the whole hold. The seeded dispute D-0912 defaults to "uphold partially" at $144 (20 % wear allowance on a 3-year-old tool).
33. **Settings publishing** is single-admin in the pilot (the design asks for a second approval); every publish archives the previous live version, mirrors cancellation policies to their table and logs an admin action; rollback creates a new live version copying the archived config so history stays linear. Drafts store the full config plus a computed path-level diff.
34. **Listing review decisions**: approve publishes immediately; request changes hides the listing (`changes_requested`), stores the checklist and pauses the SLA; reject stores the reason. Reported listings resolve their open reports on decision.
35. **Users**: the "Invite staff" button is a mailto in the pilot; suspend/reinstate and flag clearing are real and logged.

